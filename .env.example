# Discord Bot Configuration
# Copy this file to .env and fill in your actual values

# Required: Your Discord bot token from https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# Required: Your Discord application ID from https://discord.com/developers/applications
CLIENT_ID=your_client_id_here

# Optional: Default channel ID for quest announcements
# If not set, quests will only announce in channels configured per server
QUEST_CHANNEL_ID=your_default_quest_channel_id

# Optional: Database connection URL (defaults to SQLite)
DATABASE_URL=sqlite://./npcbot.sqlite

# Optional: Bot owner ID for special permissions
OWNER_ID=your_discord_user_id

# Optional: Sharding configuration
# Set to 'auto' for automatic shard count based on your bot's guild count
# Only set a specific number if you need manual control
TOTAL_SHARDS=auto

# Optional: Node.js environment
NODE_ENV=production